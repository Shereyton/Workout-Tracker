{
  "name": "workout-tracker",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "jest-environment-jsdom": "^30.0.5"
  }
}
